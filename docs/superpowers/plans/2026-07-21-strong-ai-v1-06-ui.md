# Analisi AI forte V1 — Piano 6: UI

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development
> (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use
> checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ultimo piano di V1 — card "Analisi forte" con i tre stati + due fallimenti (spec §9) e
il componente chat, montati sotto `AIAnalysisPanel` nella pagina di dettaglio bando.

**Architecture:** Due componenti client (`"use client"`), stesso stile di `AIAnalysisPanel`
esistente (nessun framework di stato esterno, `useState`/`useEffect`/`fetch` diretto, markup
semantico + classi CSS globali in `globals.css`, **nessun test unitario** — è la convenzione già
in uso per i componenti client interattivi in questo codebase: `AIAnalysisPanel` stesso non ha un
file di test, solo i componenti *presentazionali statici* sono testati con `renderToStaticMarkup`.
Questo piano si verifica con typecheck + suite esistente (zero regressioni) + **verifica manuale
nel browser** (dev server), come richiesto dalle istruzioni di progetto per le modifiche UI.

Il polling (spec §6, ~8-10s) usa `setInterval` in un `useEffect` finché `readiness === "preparing"`.
La generazione dell'analisi ricca riusa `/api/ai/analyze` (già esteso in Piano 4): appena
`readiness` diventa `ready`/`ready_partial`, il pannello lo richiama automaticamente — stesso
principio "upgrade silenzioso" del backend. Lo storico della chat va **letto** dal client per
mostrarlo al reload: aggiungo un handler `GET` alla route chat già esistente (Piano 5), stesso
file, stesso pattern di auth — non una nuova route.

## Global Constraints

- Lingua UI: italiano. Codice e commenti: inglese.
- Spec di riferimento: `docs/superpowers/specs/2026-07-20-strong-ai-analysis-rag-v1-design.md` §6, §9.
- Nessun refresh manuale: la card si auto-aggiorna via polling.
- Nessuna libreria di chat esterna: markup semplice, coerente con `AIAnalysisPanel`.
- **Interpretazione deliberata di §9**: la spec accorpa "niente allegati" e "tutti scansionati+OCR
  falliti" nello stesso messaggio di fallimento totale. Qui restano **distinti** (già distinti in
  `deriveReadiness`, Piano 4): `no_documents` → il componente non renderizza nulla (la maggior
  parte dei bandi non ha allegati PDF; mostrare una card "non disponibile" su ognuno sarebbe
  rumore). `failed_total` → allegati presenti ma illeggibili, mostra il messaggio di fallimento.
  Scelta di prodotto, non contraddizione: entrambi gli stati comunicano "niente chat", cambia solo
  se la card è visibile.
- Riusa `Button` da `@/components/ui/button` (stesso primitivo di `AIAnalysisPanel`/`SaveButton`).

---

## File Structure (Piano 6)

- Modify: `app/src/app/api/ai/strong/chat/route.ts` — aggiunge `GET` (storico).
- Modify: `app/src/app/api/ai/strong/__tests__/chat-route.test.ts` — nuovi test per `GET`.
- Create: `app/src/components/grants/strong-analysis-panel.tsx`
- Create: `app/src/components/grants/strong-chat-panel.tsx`
- Modify: `app/src/app/(app)/bandi/[id]/page.tsx` — monta `StrongAnalysisPanel`.
- Modify: `app/src/app/globals.css` — stili `.strong-panel*`/`.strong-chat*`.

---

## Task 1: `GET /api/ai/strong/chat` — storico conversazione

**Files:**
- Modify: `app/src/app/api/ai/strong/chat/route.ts`
- Modify: `app/src/app/api/ai/strong/__tests__/chat-route.test.ts`

**Interfaces:**
- Produces: `GET` handler, `{ messages: { role: "user"|"assistant"; content: string }[] }`.
  Consumato da `StrongChatPanel` (Task 3).

- [ ] **Step 1: Aggiungere i test (append, i test POST esistenti restano invariati)**

Append to `app/src/app/api/ai/strong/__tests__/chat-route.test.ts`:

```typescript
import { GET } from "../chat/route";

function get(grantId?: string): Request {
  const url = new URL("http://localhost/api/ai/strong/chat");
  if (grantId) url.searchParams.set("grantId", grantId);
  return new Request(url);
}

describe("GET /api/ai/strong/chat", () => {
  it("returns 401 when unauthenticated", async () => {
    getUser.mockResolvedValue({ data: { user: null } });
    const res = await GET(get("g1"));
    expect(res.status).toBe(401);
  });

  it("returns 400 when grantId is missing", async () => {
    getUser.mockResolvedValue({ data: { user: { id: "u1" } } });
    const res = await GET(get());
    expect(res.status).toBe(400);
  });

  it("returns the stored history in order, scoped to grant and user", async () => {
    getUser.mockResolvedValue({ data: { user: { id: "u1" } } });
    from.mockImplementation((table: string) => {
      if (table !== "chat_messages") throw new Error(`unexpected table ${table}`);
      return {
        select: () => ({
          eq: () => ({
            eq: () => ({
              order: async () => ({
                data: [
                  { role: "user", content: "Prima domanda" },
                  { role: "assistant", content: "Prima risposta" },
                ],
              }),
            }),
          }),
        }),
      };
    });

    const res = await GET(get("g1"));
    expect(res.status).toBe(200);
    const body = await res.json();
    expect(body.messages).toEqual([
      { role: "user", content: "Prima domanda" },
      { role: "assistant", content: "Prima risposta" },
    ]);
  });
});
```

- [ ] **Step 2: Eseguire il test per verificarlo fallire**

Run: `cd app && npx vitest run src/app/api/ai/strong/__tests__/chat-route.test.ts`
Expected: FAIL — `GET` non esportato da `../chat/route`.

- [ ] **Step 3: Implementare**

In `app/src/app/api/ai/strong/chat/route.ts`, aggiungere in fondo al file:

```typescript
// Chat history for display on reload (spec §5: "riprendibile tra sessioni/dispositivi"). No
// entitlement/LLM call — a plain owner-scoped read (RLS already restricts to the caller's rows).
export async function GET(request: Request): Promise<Response> {
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) return Response.json({ error: "Non autenticato." }, { status: 401 });

  const grantId = new URL(request.url).searchParams.get("grantId");
  if (!grantId) return Response.json({ error: "Richiesta non valida." }, { status: 400 });

  const { data: rows } = await supabase
    .from("chat_messages")
    .select("role, content")
    .eq("grant_id", grantId)
    .eq("user_id", user.id)
    .order("created_at");

  return Response.json({ messages: rows ?? [] }, { headers: { "Cache-Control": "no-store" } });
}
```

- [ ] **Step 4: Eseguire tutti i test del file per verificarli passare**

Run: `cd app && npx vitest run src/app/api/ai/strong/__tests__/chat-route.test.ts`
Expected: PASS (tutti — i 6 preesistenti + i 3 nuovi).

- [ ] **Step 5: Typecheck**

Run: `cd app && npx tsc --noEmit`
Expected: nessun errore.

- [ ] **Step 6: Commit**

```bash
git add app/src/app/api/ai/strong/chat/route.ts app/src/app/api/ai/strong/__tests__/chat-route.test.ts
git commit -m "feat(ai): GET /api/ai/strong/chat — conversation history for reload"
```

---

## Task 2: `StrongChatPanel`

**Files:**
- Create: `app/src/components/grants/strong-chat-panel.tsx`

**Interfaces:**
- Consumes: `GET`/`POST /api/ai/strong/chat` (Task 1, Piano 5).
- Produces: `StrongChatPanel({ grantId }: { grantId: string })`, consumato da
  `StrongAnalysisPanel` (Task 3).

- [ ] **Step 1: Implementare (nessun test unitario — convenzione dei componenti client interattivi
  di questo codebase, vedi Architecture; verificato manualmente nel browser al Task 5)**

Create `app/src/components/grants/strong-chat-panel.tsx`:

```tsx
"use client";
import { useEffect, useRef, useState } from "react";
import { Button } from "@/components/ui/button";

interface Message {
  role: "user" | "assistant";
  content: string;
}

const SUGGESTED_PROMPTS = [
  "Riassumi i requisiti principali in poche righe",
  "Il mio ente può candidarsi?",
  "Quali documenti devo preparare?",
];

export function StrongChatPanel({ grantId }: { grantId: string }) {
  const [messages, setMessages] = useState<Message[]>([]);
  const [input, setInput] = useState("");
  const [sending, setSending] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const [loaded, setLoaded] = useState(false);
  const bottomRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    fetch(`/api/ai/strong/chat?grantId=${encodeURIComponent(grantId)}`)
      .then((res) => res.json())
      .then((body) => { if (Array.isArray(body.messages)) setMessages(body.messages); })
      .finally(() => setLoaded(true));
  }, [grantId]);

  useEffect(() => {
    bottomRef.current?.scrollIntoView({ block: "nearest" });
  }, [messages]);

  async function send(text: string) {
    const question = text.trim();
    if (!question || sending) return;
    setSending(true);
    setError(null);
    setMessages((prev) => [...prev, { role: "user", content: question }]);
    setInput("");
    try {
      const res = await fetch("/api/ai/strong/chat", {
        method: "POST",
        headers: { "content-type": "application/json" },
        body: JSON.stringify({ grantId, message: question }),
      });
      const body = await res.json();
      if (!res.ok) {
        setError(typeof body.error === "string" ? body.error : "Risposta non riuscita. Riprova.");
      } else {
        setMessages((prev) => [...prev, { role: "assistant", content: body.reply as string }]);
      }
    } catch {
      setError("Risposta non riuscita. Riprova.");
    } finally {
      setSending(false);
    }
  }

  return (
    <div className="strong-chat">
      <p className="strong-chat-note">La chat tiene conto del tuo profilo ente.</p>

      {loaded && messages.length === 0 && (
        <div className="strong-chat-suggestions">
          {SUGGESTED_PROMPTS.map((p) => (
            <button
              key={p}
              type="button"
              className="strong-chat-suggestion"
              onClick={() => send(p)}
              disabled={sending}
            >
              {p}
            </button>
          ))}
        </div>
      )}

      <div className="strong-chat-messages">
        {messages.map((m, i) => (
          <div key={i} className="strong-chat-message" data-role={m.role}>
            <p>{m.content}</p>
          </div>
        ))}
        <div ref={bottomRef} />
      </div>

      {error && <p role="alert" className="form-feedback" data-type="error">{error}</p>}

      <form
        className="strong-chat-form"
        onSubmit={(e) => {
          e.preventDefault();
          send(input);
        }}
      >
        <textarea
          value={input}
          onChange={(e) => setInput(e.target.value)}
          placeholder="Fai una domanda su questo bando…"
          rows={2}
          disabled={sending}
        />
        <Button type="submit" disabled={sending || !input.trim()}>
          {sending ? "Invio…" : "Invia"}
        </Button>
      </form>
    </div>
  );
}
```

- [ ] **Step 2: Typecheck**

Run: `cd app && npx tsc --noEmit`
Expected: nessun errore.

- [ ] **Step 3: Commit**

```bash
git add app/src/components/grants/strong-chat-panel.tsx
git commit -m "feat(ui): StrongChatPanel — per-grant advisory chat component"
```

---

## Task 3: `StrongAnalysisPanel`

**Files:**
- Create: `app/src/components/grants/strong-analysis-panel.tsx`

**Interfaces:**
- Consumes: `GET /api/ai/strong/status`, `POST /api/ai/strong/prepare`, `POST /api/ai/analyze`
  (già esistenti), `ReadinessState` (`@/lib/ai/document-readiness`), `GrantAnalysis`
  (`@/lib/ai/analyze-grant`), `StrongChatPanel` (Task 2).
- Produces: `StrongAnalysisPanel({ grantId }: { grantId: string })`, montato in `page.tsx` (Task 4).

- [ ] **Step 1: Implementare (nessun test unitario, stessa motivazione del Task 2)**

Create `app/src/components/grants/strong-analysis-panel.tsx`:

```tsx
"use client";
import { useCallback, useEffect, useState } from "react";
import type { ReadinessState } from "@/lib/ai/document-readiness";
import type { GrantAnalysis } from "@/lib/ai/analyze-grant";
import { Button } from "@/components/ui/button";
import { StrongChatPanel } from "./strong-chat-panel";

const POLL_MS = 9_000;

const SECTIONS: [keyof GrantAnalysis, string][] = [
  ["puntiDiForza", "Punti di forza"],
  ["rischi", "Rischi"],
  ["suggerimenti", "Suggerimenti per la candidatura"],
  ["passiSuccessivi", "Passi successivi"],
];

export function StrongAnalysisPanel({ grantId }: { grantId: string }) {
  const [readiness, setReadiness] = useState<ReadinessState | null>(null);
  const [analysis, setAnalysis] = useState<GrantAnalysis | null>(null);
  const [error, setError] = useState<string | null>(null);
  const [starting, setStarting] = useState(false);
  const [analyzing, setAnalyzing] = useState(false);

  const fetchStatus = useCallback(async () => {
    const res = await fetch(`/api/ai/strong/status?grantId=${encodeURIComponent(grantId)}`);
    const body = await res.json();
    if (res.ok) setReadiness(body.readiness as ReadinessState);
  }, [grantId]);

  useEffect(() => {
    fetchStatus();
  }, [fetchStatus]);

  // Polling (spec §6, ~8-10s) while the extraction worker is still running.
  useEffect(() => {
    if (readiness !== "preparing") return;
    const id = setInterval(fetchStatus, POLL_MS);
    return () => clearInterval(id);
  }, [readiness, fetchStatus]);

  // Silent upgrade (spec §1): once documents are ready, /api/ai/analyze naturally returns the
  // richer analysis — no separate "strong analyze" endpoint.
  useEffect(() => {
    if (readiness !== "ready" && readiness !== "ready_partial") return;
    let cancelled = false;
    setAnalyzing(true);
    fetch("/api/ai/analyze", {
      method: "POST",
      headers: { "content-type": "application/json" },
      body: JSON.stringify({ grantId }),
    })
      .then(async (res) => {
        const body = await res.json();
        if (cancelled) return;
        if (res.ok) setAnalysis(body.analysis as GrantAnalysis);
        else setError(typeof body.error === "string" ? body.error : "Analisi non riuscita. Riprova.");
      })
      .catch(() => { if (!cancelled) setError("Analisi non riuscita. Riprova."); })
      .finally(() => { if (!cancelled) setAnalyzing(false); });
    return () => { cancelled = true; };
  }, [readiness, grantId]);

  async function start() {
    setStarting(true);
    setError(null);
    try {
      const res = await fetch("/api/ai/strong/prepare", {
        method: "POST",
        headers: { "content-type": "application/json" },
        body: JSON.stringify({ grantId }),
      });
      const body = await res.json();
      if (!res.ok) setError(typeof body.error === "string" ? body.error : "Impossibile avviare l'analisi forte.");
      else setReadiness(body.readiness as ReadinessState);
    } catch {
      setError("Impossibile avviare l'analisi forte.");
    } finally {
      setStarting(false);
    }
  }

  // Still loading, or the grant has no PDF attachments at all: nothing to show (see plan's
  // Global Constraints for why no_documents renders nothing rather than a permanent notice).
  if (readiness === null || readiness === "no_documents") return null;

  return (
    <section className="strong-panel">
      <div className="strong-panel-header">
        <h2>Analisi forte</h2>
        <p className="strong-panel-subtitle">
          Analisi basata sul testo reale dei documenti ufficiali del bando, con chat dedicata.
        </p>
      </div>

      {error && <p role="alert" className="form-feedback" data-type="error">{error}</p>}

      {readiness === "not_started" && (
        <Button type="button" onClick={start} disabled={starting}>
          {starting ? "Avvio in corso…" : "Avvia analisi forte"}
        </Button>
      )}

      {readiness === "preparing" && (
        <p className="strong-panel-status">
          Stiamo leggendo i documenti del bando — circa 1 minuto. Puoi restare o tornare più tardi.
        </p>
      )}

      {readiness === "failed_total" && (
        <p className="strong-panel-status">
          Questo bando non ha documenti leggibili automaticamente.
        </p>
      )}

      {(readiness === "ready" || readiness === "ready_partial") && (
        <>
          {readiness === "ready_partial" && (
            <p className="strong-panel-status">
              Alcuni allegati non erano leggibili automaticamente: procediamo con quanto disponibile.
            </p>
          )}
          {analyzing && <p className="strong-panel-status">Generazione dell'analisi in corso…</p>}
          {analysis && (
            <div className="ai-panel-sections">
              <p className="strong-panel-badge">Analisi basata sui documenti ufficiali</p>
              {SECTIONS.map(([key, title]) =>
                analysis[key].length > 0 ? (
                  <div key={key}>
                    <h3>{title}</h3>
                    <ul>{analysis[key].map((line, i) => <li key={i}>{line}</li>)}</ul>
                  </div>
                ) : null,
              )}
            </div>
          )}
          <StrongChatPanel grantId={grantId} />
        </>
      )}
    </section>
  );
}
```

- [ ] **Step 2: Typecheck**

Run: `cd app && npx tsc --noEmit`
Expected: nessun errore.

- [ ] **Step 3: Commit**

```bash
git add app/src/components/grants/strong-analysis-panel.tsx
git commit -m "feat(ui): StrongAnalysisPanel — 3 states + 2 failures card (spec §9)"
```

---

## Task 4: Montare il pannello + stili

**Files:**
- Modify: `app/src/app/(app)/bandi/[id]/page.tsx`
- Modify: `app/src/app/globals.css`

- [ ] **Step 1: Montare `StrongAnalysisPanel` sotto `AIAnalysisPanel`**

In `app/src/app/(app)/bandi/[id]/page.tsx`, aggiungere l'import:

```typescript
import { StrongAnalysisPanel } from "@/components/grants/strong-analysis-panel";
```

E sostituire la chiusura del componente:

```tsx
      <AIAnalysisPanel grantId={grant.id} />
      <StrongAnalysisPanel grantId={grant.id} />
    </main>
  );
}
```

- [ ] **Step 2: Aggiungere gli stili**

In `app/src/app/globals.css`, dopo il blocco `AI ANALYSIS PANEL` (dopo la riga
`.ai-panel-sections li { margin-bottom: 0.25rem; }`), aggiungere:

```css
/* ------------------------------------------------------------------ */
/* STRONG ANALYSIS PANEL + CHAT                                        */
/* ------------------------------------------------------------------ */
.strong-panel {
  margin-top: 1.5rem;
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  background: var(--surface-raised);
  box-shadow: var(--shadow-sm);
  padding: 1rem;
}
.strong-panel-header h2 { margin: 0 0 0.25rem; font-size: 0.9375rem; }
.strong-panel-subtitle {
  font-size: 0.8125rem;
  color: var(--text-muted);
  margin: 0 0 0.75rem;
}
.strong-panel-status {
  font-size: 0.875rem;
  color: var(--text-secondary);
  padding: 0.5rem 0;
}
.strong-panel-badge {
  font-size: 0.75rem;
  color: var(--text-muted);
  background: var(--success-light);
  display: inline-block;
  padding: 0.125rem 0.5rem;
  border-radius: var(--radius-sm);
  margin-bottom: 0.5rem;
}

.strong-chat {
  margin-top: 1rem;
  border-top: 1px solid var(--border);
  padding-top: 1rem;
}
.strong-chat-note {
  font-size: 0.75rem;
  color: var(--text-muted);
  font-style: italic;
  margin: 0 0 0.75rem;
}
.strong-chat-suggestions {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
  margin-bottom: 0.75rem;
}
.strong-chat-suggestion {
  font-size: 0.8125rem;
  padding: 0.375rem 0.75rem;
  border: 1px solid var(--border);
  border-radius: var(--radius);
  background: var(--surface);
  cursor: pointer;
}
.strong-chat-suggestion:hover { border-color: var(--border-hover); }
.strong-chat-messages {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
  max-height: 24rem;
  overflow-y: auto;
  margin-bottom: 0.75rem;
}
.strong-chat-message p {
  margin: 0;
  padding: 0.5rem 0.75rem;
  border-radius: var(--radius);
  font-size: 0.875rem;
  max-width: 85%;
}
.strong-chat-message[data-role="user"] {
  align-self: flex-end;
}
.strong-chat-message[data-role="user"] p {
  background: var(--primary-light);
  color: var(--text);
}
.strong-chat-message[data-role="assistant"] p {
  background: var(--surface);
  border: 1px solid var(--border);
}
.strong-chat-form {
  display: flex;
  gap: 0.5rem;
  align-items: flex-end;
}
.strong-chat-form textarea { flex: 1; resize: vertical; }
```

- [ ] **Step 3: Typecheck e suite completa**

Run: `cd app && npx tsc --noEmit && npm test`
Expected: nessun errore, tutti i test passano.

- [ ] **Step 4: Commit**

```bash
git add "app/src/app/(app)/bandi/[id]/page.tsx" app/src/app/globals.css
git commit -m "feat(ui): mount StrongAnalysisPanel on the grant detail page"
```

---

## Task 5: Verifica manuale nel browser

**Files:** nessuna modifica di codice.

- [ ] **Step 1: Avviare il dev server**

Run: `cd app && npm run dev`

- [ ] **Step 2: Aprire una pagina di dettaglio bando come utente autenticato con profilo**

Verificare:
- Bando **senza allegati PDF**: nessuna card "Analisi forte" visibile (comportamento atteso,
  vedi Global Constraints).
- Bando **con allegati PDF**, mai analizzato: card visibile, stato "Avvia analisi forte".
- Click sul bottone: passa a "Stiamo leggendo i documenti…" (il worker Piano 3 è **spento** di
  default in produzione — verificare solo che lo stato UI cambi correttamente, non che completi;
  se si vuole testare l'intero ciclo, impostare i Vault secret dell'estrazione o chiamare
  manualmente `POST /api/cron/extract-documents` con l'header `Authorization: Bearer $CRON_SECRET`).
- Nessun errore console per componenti/hook (polling che parte/si ferma correttamente,
  nessun "memory leak" warning di React per update dopo unmount).

- [ ] **Step 2 (nota):** questa verifica manuale conferma l'integrazione UI reale, non sostituibile
  da typecheck/test automatici (linea guida di progetto per le modifiche UI). Riportare
  esplicitamente all'utente cosa è stato verificato e cosa richiede setup aggiuntivo (OCR_SPACE_API_KEY,
  Vault secret) per il ciclo end-to-end completo in produzione.

---

## Task 6: Verifica finale + documentazione

- [ ] **Step 1: Suite completa app + scraper**

Run: `cd app && npx tsc --noEmit && npm test && cd ../scraper && npm run typecheck`
Expected: tutto verde.

- [ ] **Step 2: Commit del piano**

```bash
git add docs/superpowers/plans/2026-07-21-strong-ai-v1-06-ui.md
git commit -m "docs(plan): strong AI analysis V1 — plan 6/6 (UI) — V1 COMPLETE"
```

---

## Self-Review (Piano 6)

- **Copertura spec:** §9 tre stati + due fallimenti → `StrongAnalysisPanel` (Task 3) copre
  `not_started`/`preparing`/`ready`+`ready_partial`/`failed_total`; `no_documents` gestito come
  deviazione di prodotto documentata. §6 polling ~8-10s, no refresh manuale → `useEffect` +
  `setInterval(fetchStatus, 9000)` mentre `preparing`, più il check di stato al load. §5 chat
  "riprendibile tra sessioni" → `GET` storico (Task 1) + `StrongChatPanel` lo carica al mount.
  "3-4 prompt suggeriti" → `SUGGESTED_PROMPTS`. "nota tiene conto del tuo profilo" →
  `strong-chat-note`. Coperto — **V1 completo** (Piani 1-6).
- **Fuori scope (corretto):** V2 (chat cross-bando, crediti) — documento separato, non iniziato
  per design (spec: "da avviare quando V1 sarà in produzione").
- **Placeholder:** nessun TBD; codice completo in ogni step. La verifica manuale (Task 5) è
  intenzionalmente non automatizzabile, non un placeholder.
- **Consistenza tipi:** `ReadinessState` importato identico da `@/lib/ai/document-readiness`
  in `StrongAnalysisPanel`. `GrantAnalysis`/`SECTIONS` identici a quelli di `AIAnalysisPanel`
  (stessa forma, stesso schema condiviso). Le route chiamate (`/api/ai/strong/status`,
  `/api/ai/strong/prepare`, `/api/ai/strong/chat` GET+POST, `/api/ai/analyze`) esistono tutte
  e sono già testate nei piani precedenti.
